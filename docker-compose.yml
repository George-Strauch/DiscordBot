services:
  bot:
    build: .
    container_name: discord-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - /opt/bot/data:/opt/bot/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
